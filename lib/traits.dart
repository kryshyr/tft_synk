import 'package:flutter/material.dart';
import 'app_constants.dart';

class TraitsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text('Traits Content', style: AppTextStyles.bodyText1BeaufortforLOL),
    );
  }
}
