import 'package:flutter/material.dart';
import 'app_constants.dart';

class ItemsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text('Items Content', style: AppTextStyles.bodyText1BeaufortforLOL),
    );
  }
}
