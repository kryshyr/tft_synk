import 'package:flutter/material.dart';
import 'app_constants.dart';

class UnitTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Unit Content', style: AppTextStyles.bodyText1BeaufortforLOL),
    );
  }
}
